import React from "react";
import Header from "./Header";

const Dashboard = () => (
    <div className="dashboard-main-container">
        <Header/>
        <div className="landing-page">
            This is the landing Page area
        </div>

        <div className="dashboard-page">
        Dashboard
        </div>

    </div>
);

export default Dashboard;